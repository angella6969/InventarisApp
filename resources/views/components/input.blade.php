<input placeholder={{ $placeholder }} 
        type="text" 
        name= {{ $name }} 
        class={{ $class }} 
        id={{ $name }}
        required 
        value={{ $value }} >
        {{-- value="{{ old({{ $name }}) }}" > --}}